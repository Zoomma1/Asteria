version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: AsteriaDB
    restart: on-failure
    environment:
      POSTGRES_USER: Asteria
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: AsteriaDB
    ports:
      - "5434:5432"
    volumes:
      - pg_data_asteria:/var/lib/postgresql/data

  backend:
    build: ./backend
    depends_on:
      - db
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/AsteriaDB
      SPRING_DATASOURCE_USERNAME: Asteria
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/logs

volumes:
  pg_data_asteria:
